## CSE 331 HW6 Questions

NOTE: This file is used as part of HW6, and is not due as part of the HW5
submission. You may leave it blank for HW5.

## Part 1
###########################################################################

A few examples of possible Graph representations:

    A: A collection of nodes and a collection of edges.
    B: An adjacency list, in which each node is associated with a list of its outgoing edges.
    C: An adjacency matrix, which explicitly represents, for every pair ⟨A,B⟩ of nodes, whether there 
       is a link from A to B, and how many.

1. In two or three sentences, explain an advantage and a disadvantage of each of the
representations listed above (for example, in terms of runtime complexity, space
complexity, or ease of implementation).

ADVANTAGE (A): Implementation is relatively easy for methods like addNode() and addEdge().
All I need to do is add them to the collections!

DISADVANTAGE (A): Lots of methods, such as removeNode() run in linear time. removeNode()
requires iterating through the entire collection of edges to look for any edges that
point to or away from the removed node.

ADVANTAGE (B): Methods such as listChildren() are much quicker. listChildren() is easier
since we can look at the list of outgoing edges.

DISADVANTAGE (B): Harder to implement. Also, removeNode() still suffers from linear time
because we also have to delete INCOMING edges, which aren't part of the adjacency list.

ADVANTAGE (C): removeEdge() would be supremely easy to implement. Just remove a link!
removeNode() would also be easy, as I could remove all links and then remove entries containing
the removed node from the matrix (might still be linear time, though).

DISADVANTAGE (C): Holy cow this would take up so much space. Especially when the amount of nodes
gets really large.




2. In two to three sentences, describe the representation you chose and explain why you
chose it. If you chose to use a different representation than one of the three we described
above, be sure to list its advantages and disadvantages, too.

I chose option A for the ease of implementation. This also takes the least space out
of all the representation options. The RI is also easier to check.




## Part 4
###########################################################################

    (Please keep your answers brief and to-the-point.)

1. Describe any new tests you added and why you added them, or why you feel that your original
tests alone are sufficient.

I decided to properly implement an equals and hash method, so I wrote junit tests for them.
I also created a more complicated constructor and wrote tests for them as well.
Since I now had an equals method, I also cleaned up the junit tests I wrote originally.




2. Did you make any changes to your specifications as you were implementing your Graph? (yes/no)
If yes, describe your changes and why you made them.
My listChildren() spec was misleading, so I rewrote it to specify that it also includes edges
leading to the children and that they appear in alphabetical order.
I also specified that listNodes() appears in alphabetical order.



